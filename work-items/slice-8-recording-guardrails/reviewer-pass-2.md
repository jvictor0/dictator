# Reviewer pass 2

## Findings
- No P0/P1 findings.
- Fallback scope is constrained to coding apps (`Codex`/`Cursor`) and only when AX focused-element read fails.
- Default strict behavior for non-coding apps remains unchanged.

## Decision
- Approved.
